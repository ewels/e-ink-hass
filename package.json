{
  "name": "eink-dashboard-cards",
  "version": "0.1.0",
  "description": "Custom Home Assistant Lovelace cards for a Seeed reTerminal E1002 e-ink dashboard.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --serve",
    "typecheck": "tsc --noEmit",
    "snapshot": "node scripts/snapshot.mjs"
  },
  "dependencies": {
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3",
    "@types/node": "^26.0.0"
  }
}
